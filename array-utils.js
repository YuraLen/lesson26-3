export function firstFunction(arr) {
  let newArr = [];
  newArr = arr.concat()
  return newArr = newArr.sort((a, b) => a - b)
}

export function secondFunction(arr) {
  let newArr = [];
  newArr = arr.concat()
  return newArr = newArr.sort((a, b) => b - a)
};